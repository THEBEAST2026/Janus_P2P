# Janus P2P Backend Environment Variables

# Redis (optional — falls back to in-memory)
REDIS_URL=redis://localhost:6379

# LSTM model weights path
LSTM_MODEL_PATH=ml/conjunction_lstm.pt

# Set to 1 to fetch real ISRO TLEs from CelesTrak on startup
ISRO_SEED=0
